# 阶段3 开发清单（后端接入 / API / 智学 Agent）

> 进度图例：✅ 已完成并验收　🔧 待开发　⏳ 进行中
> 架构：Supabase 直连（前端 supabase-js + Auth + RLS）；智学走 minimax 云函数；封面/音视频在阿里云 OSS（存 URL）。
> 更新：2026-06-09

---

## ✅ 已完成（3.1 ~ 3.5，已真机验收 20/20）

- ✅ **3.1 前置**：装 `@supabase/supabase-js`、`lib/supabase/client.ts`（浏览器 anon 单例）、`.env.local`（URL / anon / service_role）、连接+写库自测脚本。
- ✅ **3.2 导书**：`scripts/import-books.mjs`（干跑校验 + 非覆盖/覆盖两模式）、`import-books` Skill、格式文档、`db-stats` 回读。已导入《清醒地活》《认知觉醒》。支持 `no:0`=前言。
- ✅ **3.4 目录读取**：`lib/api.ts` 首页/分类/详情/章节/搜索/乱翻 全部查 Supabase（行→Book/Chapter 映射、封面走 OSS、tags 前插分类名、热门好书按 shelved_at）。
- ✅ **3.3 鉴权**：`useAuth` 接 Supabase Auth（注册/登录/退出/会话恢复）、LoginSheet 邮箱+密码+昵称、体验账号 `demo@ailibrary.app`、RLS 本人隔离。
- ✅ **3.5 用户数据**：收藏/笔记/书评/历史/文字进度/音视频进度/学习时长 全部「本地乐观更新 + 写穿透 Supabase」；登录加载本人数据；退出清空。
- ✅ 验收中修复：手机 HTTP 非安全上下文（uuid/clipboard）、阅读器进度口径、mock 残留、hydration 报错。

---

## 🔧 待开发（剩余 4 块）

### 🔧 ⑥ 计数触发器（小 · 纯 SQL）
让 books 上「存但不展示」的计数列变真（现在恒为 0）。均用 `SECURITY DEFINER` 触发器/RPC。
- [ ] `fav_count`（收藏人数=点赞数）：favorites 表 after insert/delete → 回写 books.fav_count
- [ ] `review_count` + `rating`：reviews 表 after insert/update/delete → 回写条数 + 均分（round 1 位）
- [ ] `readers`（在读+读完人数）：reading_history/text_progress/media_progress 的 distinct user 数 → 定时任务/视图回写
- [ ] `read_count`（阅读次数，不去重）：后端 RPC `read_count = read_count + 1`，需先定义「何为一次阅读」
- ⚠️ 触发器务必 `SECURITY DEFINER` + `set search_path=public`（否则被 books 的 RLS 拦）
- 产出：一段 SQL 贴 SQL Editor 跑；交付前我先核对。

### 🔧 ⑦ 智学 Agent「小涤」（大 · 核心功能 · 需 minimax 密钥）
把 mock 对话（`buildChatReply`）换成真实 Agent。
- [ ] 建一个**云函数**（Vercel serverless/edge）代理 minimax，保护 API key（不暴露前端）
- [ ] System 变量注入：图书馆书单 / 用户已读·收藏·在读书单 / 学习总时长 / 压缩历史
- [ ] 5 个工具：① 推荐书目 ② 读某书目录 ③ 读章内容 ④ 引用章节 ⑤ 联网搜索
- [ ] 上下文压缩：≥500K 压到约 10K，保留最近 20 轮
- [ ] 流式输出（前端假流式 → 真流式）
- [ ] 对话持久化：`chat_sessions`（现在是本地 `useChat`，要落库 + compressed_history/compressed_until）
- 依赖：**你的 minimax TokenPlan 密钥**（放 `.env.local`，仅云函数用）

### 🔧 ⑧ 乱翻日更 + 热门搜索（中 · 需定时任务）
- [ ] 乱翻 `flip_feed`：每天凌晨定时（Vercel Cron）调 minimax 为每人生成 ≤50 本「猜你想读」入库；前端 `getFlip` 改读 flip_feed（现在是临时取有视频书洗牌）
- [ ] 热门搜索：前端 search 时写 `search_logs`；后端聚合 Top20（命中现有书的 title/author/tags）；替换现在 mock 的 `hotSearches`
- 依赖：Vercel Cron（或其它定时器）+ minimax

### 🔧 补 账号小功能（小~中）
- [ ] 设置「修改密码」：`supabase.auth.updateUser({ password })`（现为本地弹层）
- [ ] 设置「注销账号」：需一个**删号云函数**（service_role 删 auth.users，级联清数据）
- [ ] 头像上传：补 `avatars` 桶**写策略**（本人可传/改/删，路径 `<uid>/...`）+ 前端 `supabase.storage` 上传 + 写 `profiles.avatar_url`（现为预设头像 a1–a8）

---

## 阶段3 完成后 → 阶段4
整体联调 / 测试 / 验收 / 部署上线（Vercel，绑定环境变量；OSS 跨域 CORS 核对）。

## 备注
- 当前 4 个后端提交都在本地 main（`a1463d5`→`aa49179`），**尚未 push**；需部署时再推送。
- 读者评价（他人书评）本版数据-only、不前端展示，无新开发任务。
- 建议推进顺序：**⑥（快）→ ⑦（智学，重头）→ ⑧ → 补**。
