# T1 证据：数据库安全收紧执行与实测（2026-06-11）

- SQL 全文：docs/后端_T1安全收紧SQL.md（5 段，run-sql.mjs 直连执行全部成功）
- 收紧后授权快照：grants-after.txt（anon 各表仅 SELECT + search_logs INSERT；authenticated 用户数据表仅 DML；
  profiles 收至 SELECT + 列级 UPDATE(nickname,bio,avatar_seed,avatar_url)）

## 攻击路径实测（scripts/verify-t1-security.mjs，7/7 通过）

```
✅ ① anon 伪造他人 uid 写 search_logs 被拒（42501）
✅ ①b 游客匿名搜索上报仍正常
✅ ② anon 调 refresh_books_readers 被拒（42501）
✅ ③ 登录用户直改 read_seconds 被拒（只能走只增 RPC）（42501）
✅ ④ 登录用户改 nickname 正常（列级授权放行）
✅ ⑤ anon 写 favorites 被拒（表级写权限已收）（42501）
✅ ⑥ 注册 metadata 昵称经触发器落 profiles（实际=收紧验证员）
```

## 排查插曲（自测诚实记录）

首跑 ⑤ 曾报"竟然成功了"——排查（probe-fav-insert.mjs 独立复测：42501 + 落行 0）确认**安全收紧本身有效**，
是验证脚本缺陷：③④ 在同一个 anon 客户端实例上 signInWithPassword 登录了 demo，到 ⑤ 时该实例已带登录态，
插的是"本人收藏"（RLS 本就允许）。已修：登录改用独立客户端实例，重跑 7/7 全过。
教训：RLS/权限类断言中，匿名客户端实例必须全程保持匿名。
