# AI 图书馆 · 项目待办开发清单（直到上线）

> 生成日期：2026-06-10。依据：`阶段3开发清单.md` + `后端开始前的功能清单-完备版.md`（以实际代码为准）+ `AI图书馆功能详细说明补充.md`（后端业务参考），并已逐项核对当前代码。
> 用法：**一条一条做，每条「开发 → 自测 → 你真机验收通过 → 打勾」再进下一条。**
> 图例：⬜ 待做　⏳ 进行中　✅ 完成已验收
> 架构不变：Supabase 直连（supabase-js + Auth + RLS）；大模型一律走 **minimax TokenPlan**；封面/音视频在阿里云 OSS；Supabase 只有 `avatars` 一个桶。

---

## 已完成回顾（不再动）

- ✅ 前端全部 4 模块（智学外壳/泡馆/乱翻/我的）+ 设计系统 + 真机验收
- ✅ 阶段2：Supabase 14 表建库 + RLS + 触发器(updated_at/handle_new_user)
- ✅ 阶段3.1~3.5：supabase 接入 / 导书脚本(已导2本) / 目录读取 / 鉴权 / 用户数据云同步（真机 20/20 验收）
- ✅ 专项修复：乱翻 3 视频池架构（100+ 本不崩）、详情页播放器、阅读器 Markdown 渲染（含 CJK 加粗 bug）、29 项功能 bug
- ⚠️ 注意：后端相关 commit 都在本地 main，**尚未 push**，部署前统一推送

---

## 第一批｜T1 计数触发器（小 · 纯 SQL · 半天）

> 目标：books 上「存但不展示」的计数列变真（现在恒 0）。产出一段 SQL，贴 Supabase SQL Editor 执行。全部 `SECURITY DEFINER` + `set search_path=public`（否则被 books 的 RLS 拦）。

- ✅ **T1.1** `fav_count`：favorites 触发器回写（2026-06-10 用户验收通过）
- ✅ **T1.2** `rating` + `review_count`：reviews 触发器回写均分+条数（验收通过）
- ✅ **T1.3** `reviews.likes`：review_likes 触发器聚合（验收通过）
- ✅ **T1.4** `readers`：pg_cron 每日 19:30 UTC（北京 03:30）重算（验收通过）
- ✅ **T1.5** `read_count`：RPC + 前端四处埋点，口径=单次会话真实阅读/播放累计≥30秒记1次（验收通过）
- 产出：`docs/后端_T1计数触发器SQL.md`（已在 SQL Editor 执行）；前端埋点 `lib/useReadingClock.ts:useReadCountBump`
- 🐞➡✅ **验收发现 bug（已修复，真实浏览器三场景验证通过，commit e191ba2，待你真机复验）**：没读完的章退出阅读器也被标「已读」。根因=退出时 effect cleanup 里 `scrollRef` 已为 null → `max=0` 误命中「内容不足一屏即读毕」；切章 cleanup 还会用新章高度误判旧章。修复=cleanup 只落进度永不判读毕，读毕只由活跃路径（滚动≥95% / 5s 定时器测不足一屏）判定；验证脚本 `scripts/verify-chapter-read.mjs`（断言走详情页 DOM 勾）
- 🧹 测试期脏数据已清：`scripts/reset-text-read.mjs`（盘点→点名清理→核验，3 账号 9 行进度归零）
- ➕ **乱翻新增播放进度条（2026-06-11，待真机验收）**：抖音式底部细线（rAF 直写 DOM 零重渲染），按下变粗可拖动定位、拖动中显「当前/总时长」气泡，按下瞬间锁定目标视频（拖动中滑屏不串台）；seek 后重置真实播放基准并立即落续播位置（暂停态拖动也同步）。信息浮层底距 16→24px 让位命中区
- 🔍 **乱翻专项 Review（2026-06-11，修 7 处，待真机复验）**：①(用户报)乱翻↔详情页进度双向不同步——乱翻补写 `setMediaProgress` + 进入续播 seek；②(严重·数据级)滑到预加载条时 playedSec 基线未还原→新书继承旧书秒数，mediaPlayed 只增不可逆地虚高、划过的书误入历史/误判已读——新增 `primeActive()` 在「切条 effect + onLoadedMetadata」双时序还原基线，切条先清零；③卸载时最后一段观看不落历史——writeMedia 去掉对视频元素的依赖+卸载 flush；④切后台被强制复播/回前台不续播——onPause 加可见性护栏 + visibilitychange 续播/落账；⑤静音钮强行取消用户暂停——开声只管声音；⑥flipCache 不随账号切换失效——缓存按 uid 隔离；⑦坏源时静音兜底失败只转圈——补 setVErr 走错误兜底。结构验证：3 解码器恒定 + 跟手滚动无回归

## 第二批｜T2 智学 Agent「小涤」（大 · 核心重头 · 预计 3~5 个工作段）

> 把 `buildChatReply` 假流式换成真 Agent。**依赖：你的 minimax TokenPlan API Key**（放 `.env.local`，仅服务端用，绝不进前端/git）。

- ⬜ **T2.0 数据前提**：为现有书批量生成 `books.ai_digest`（200字概要）+ `chapters.ai_summary`（100字章概要）——写脚本调 minimax 生成入库；并入 `import-books` 流程（以后导新书自动生成）
- ⬜ **T2.1 云函数代理**：新建 `app/api/chat/route.ts`（Vercel serverless，Node runtime）代理 minimax，密钥只在服务端；先打通「一问一答」
- ⬜ **T2.2 System Instruction + 6 变量**：小涤角色设定（荐书/答疑/解读原文+通用问答引导）+ 注入：①图书馆书单 ②已读完书单(含笔记/书评) ③收藏书单 ④在读书单(含笔记/书评) ⑤学习总时长 ⑥压缩历史
- ⬜ **T2.3 5 个工具**：①推荐书目卡片(回book_id列表→前端拉卡) ②读取书本目录(书+各章ai_summary) ③读取章内容(chapters.content) ④引用章节卡片(book_id+章序号→前端拉卡) ⑤联网搜索（先调研 minimax 是否自带，不支持评估火山等）。工具调用中文案：查找书籍… / 翻阅图书… / 章节浏览…
- ⬜ **T2.4 真流式 SSE**：前端假流式(setInterval打字机)换成边生成边推送；「停止生成」=中断 AgentLoop；工具触发即下发卡片信号
- ⬜ **T2.5 对话持久化**：会话落 `chat_sessions`（messages jsonb；卡片只存 book_id+章序号，渲染时 JOIN）；历史对话页从云端拉取/搜索/删除/清空；赞踩+踩原因随 message 落库
- ⬜ **T2.6 上下文管理**：≥500K 异步压缩 Agent 压到约10K（最近20轮不压），写 compressed_history/compressed_until；tool_result 只留最近20轮，更早替换为「【由于上下文过长，该工具结果已被清除】」
- 验收：真机对话——荐书出推荐卡、答疑/解读出引用卡且跳转正确、流式顺滑可停止、刷新后历史还在、多轮有记忆

## 第三批｜T3 乱翻日更 + 热门搜索（中 · 需定时任务）

- ⬜ **T3.1 flip_feed 日更**：Vercel Cron 每天凌晨调 minimax 为每人生成 ≤50 本「猜你想读」（排除已读、在读优先、参考对话偏好）写入 flip_feed（service_role）
- ⬜ **T3.2 getFlip 改读 flip_feed**：50 本轮询续拉；**新用户当天无数据 → 回退最新入库 50 本（has_video=true）**
- ⬜ **T3.3 search_logs 上报**：前端每次有效搜索写一条（游客 user_id 可空）
- ⬜ **T3.4 热门搜索 Top20**：聚合 search_logs（仅保留命中现有书 title/author/tags 的词），替换 mock 的 12 个写死词
- 依赖：Vercel Cron 要等部署后才能真跑——本地先把生成脚本/接口写好，部署后启用
- 验收：乱翻信息流来自 flip_feed；搜几个词后热门搜索出现真实聚合词

## 第四批｜T4 账号小功能补全（小~中）

- ⬜ **T4.1 修改密码**：原密码用 `signInWithPassword` 校验 → `supabase.auth.updateUser({password})`（现为纯本地弹层）
- ⬜ **T4.2 注销账号**：删号云函数 `app/api/account/delete`（service_role 删 auth.users，级联清 profiles/用户数据）
- ⬜ **T4.3 头像上传**：补 `avatars` 桶写策略（本人 `<uid>/` 路径可传改删）+ 前端 storage 上传 + 写 `profiles.avatar_url`（现为本地 dataURL 预览）
- ⬜ **T4.4 意见反馈落库**（可选）：建 feedback 表只写不读；不做则维持现状 toast
- 验收：真机改密后旧密码登不上新密码能登；注销后无法登录且数据清除；换头像后换设备登录仍显示

## 第五批｜T5 阶段4 联调 · 测试 · 上线

- ⬜ **T5.1 全量回归**：按 4 模块过一遍完整流程（游客+登录双角色），含乱翻 100+ 本压力、弱网、微信内置浏览器
- ⬜ **T5.2 push 全部本地提交**（后端 commit 一直没推）
- ⬜ **T5.3 Vercel 部署**：绑定环境变量（NEXT_PUBLIC_SUPABASE_URL/anon 公开；SUPABASE_SERVICE_ROLE_KEY/MINIMAX_API_KEY 仅服务端）；启用 Cron
- ⬜ **T5.4 OSS CORS 核对**：正式域名跨域可播视频/音频/封面
- ⬜ **T5.5 上线后真机验收** + 隐私政策文案更新（「数据存本地」→ 云端口径）
- 验收：线上域名真机全流程跑通 = 项目交付

## 并行内容线｜T6 批量导书（你出内容，我出工具）

- ⬜ **T6.1** 100+ 本书内容生产：每本=文稿(分章 Markdown)+封面+视频+音频 → 传 OSS → `import-books` 脚本入库（已支持干跑校验/覆盖模式）
- ⬜ **T6.2** 导书时自动生成 ai_digest/ai_summary（依赖 T2.0 并入）
- 节奏：随时可做，不阻塞 T1~T5；建议 T2 验收后开始批量灌

## 可选/延后池（不阻塞上线，上线后再议）

- ⬜ PWA 真实图标：192/512 PNG（codex image_gen 生成）替换 icon.svg 占位
- ⬜ 语音输入 ASR（智学长按说话，现为占位 toast）
- ⬜ F68 路由组重构 / F41 长列表虚拟化（工程优化）
- ⬜ 弱网预加载下一条视频 / 清晰度切换
- ⬜ 读者评价（他人书评）公开展示（本版数据-only，将来建只读视图/count RPC）

---

## 推进顺序（定稿）

**T1（快，半天）→ T2（重头，分 7 小步逐步验收）→ T3 → T4 → T5 上线**；T6 内容线并行。
每完成一条：我自测（脚本/真实浏览器验证）→ 你真机验收 → 本文档打勾 ✅ → 下一条。
