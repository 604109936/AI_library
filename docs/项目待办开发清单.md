# AI 图书馆 · 项目待办开发清单（直到上线）

> 生成日期：2026-06-10。依据：`阶段3开发清单.md` + `后端开始前的功能清单-完备版.md`（以实际代码为准）+ `AI图书馆功能详细说明补充.md`（后端业务参考），并已逐项核对当前代码。
> 用法：**一条一条做，每条「开发 → 自测 → 你真机验收通过 → 打勾」再进下一条。**
> 图例：⬜ 待做　⏳ 进行中　✅ 完成已验收
> 架构不变：Supabase 直连（supabase-js + Auth + RLS）；大模型一律走 **minimax TokenPlan**；封面/音视频在阿里云 OSS；Supabase 只有 `avatars` 一个桶。

---

## 已完成回顾（不再动）

- ✅ 前端全部 4 模块（智学外壳/泡馆/乱翻/我的）+ 设计系统 + 真机验收
- ✅ 阶段2：Supabase 14 表建库 + RLS + 触发器(updated_at/handle_new_user)
- ✅ 阶段3.1~3.5：supabase 接入 / 导书脚本(已导2本) / 目录读取 / 鉴权 / 用户数据云同步（真机 20/20 验收）
- ✅ 专项修复：乱翻 3 视频池架构（100+ 本不崩）、详情页播放器、阅读器 Markdown 渲染（含 CJK 加粗 bug）、29 项功能 bug
- ⚠️ 注意：后端相关 commit 都在本地 main，**尚未 push**，部署前统一推送

---

## 第一批｜T1 计数触发器（小 · 纯 SQL · 半天）

> 目标：books 上「存但不展示」的计数列变真（现在恒 0）。产出一段 SQL，贴 Supabase SQL Editor 执行。全部 `SECURITY DEFINER` + `set search_path=public`（否则被 books 的 RLS 拦）。

- ✅ **T1.1** `fav_count`：favorites 触发器回写（2026-06-10 用户验收通过）
- ✅ **T1.2** `rating` + `review_count`：reviews 触发器回写均分+条数（验收通过）
- ✅ **T1.3** `reviews.likes`：review_likes 触发器聚合（验收通过）
- ✅ **T1.4** `readers`：pg_cron 每日 19:30 UTC（北京 03:30）重算（验收通过）
- ✅ **T1.5** `read_count`：RPC + 前端四处埋点，口径=单次会话真实阅读/播放累计≥30秒记1次（验收通过）
- 产出：`docs/后端_T1计数触发器SQL.md`（已在 SQL Editor 执行）；前端埋点 `lib/useReadingClock.ts:useReadCountBump`
- 🐞➡✅ **验收发现 bug（已修复，commit e191ba2，2026-06-11 用户真机验收通过）**：没读完的章退出阅读器也被标「已读」。根因=退出时 effect cleanup 里 `scrollRef` 已为 null → `max=0` 误命中「内容不足一屏即读毕」；切章 cleanup 还会用新章高度误判旧章。修复=cleanup 只落进度永不判读毕，读毕只由活跃路径（滚动≥95% / 5s 定时器测不足一屏）判定；验证脚本 `scripts/verify-chapter-read.mjs`（断言走详情页 DOM 勾）
- 🧹 测试期脏数据已清：`scripts/reset-text-read.mjs`（盘点→点名清理→核验，3 账号 9 行进度归零）
- ➕ **乱翻新增播放进度条（2026-06-11，用户验收通过 ✅）**：抖音式底部细线（rAF 直写 DOM 零重渲染），按下变粗可拖动定位、拖动中显「当前/总时长」气泡，按下瞬间锁定目标视频（拖动中滑屏不串台）；seek 后重置真实播放基准并立即落续播位置（暂停态拖动也同步）。信息浮层底距 16→24px 让位命中区
- 🔍 **乱翻专项 Review（2026-06-11，修 7 处，用户验收通过 ✅；附：底部信息字号整体缩小不挡字幕 ✅）**：①(用户报)乱翻↔详情页进度双向不同步——乱翻补写 `setMediaProgress` + 进入续播 seek；②(严重·数据级)滑到预加载条时 playedSec 基线未还原→新书继承旧书秒数，mediaPlayed 只增不可逆地虚高、划过的书误入历史/误判已读——新增 `primeActive()` 在「切条 effect + onLoadedMetadata」双时序还原基线，切条先清零；③卸载时最后一段观看不落历史——writeMedia 去掉对视频元素的依赖+卸载 flush；④切后台被强制复播/回前台不续播——onPause 加可见性护栏 + visibilitychange 续播/落账；⑤静音钮强行取消用户暂停——开声只管声音；⑥flipCache 不随账号切换失效——缓存按 uid 隔离；⑦坏源时静音兜底失败只转圈——补 setVErr 走错误兜底。结构验证：3 解码器恒定 + 跟手滚动无回归

## 第二批｜T2 智学 Agent「小涤」（大 · 核心重头 · 预计 3~5 个工作段）

> 把 `buildChatReply` 假流式换成真 Agent。**依赖：你的 minimax TokenPlan API Key**（放 `.env.local`，仅服务端用，绝不进前端/git）。

- ✅ **T2.0 数据前提**：无需生成——用户导书时已自带（核查 2026-06-11：7 本书 ai_digest 全有；101 章中仅 6 个「前言(no:0)」缺 ai_summary → Agent 拼装时代码兜底，核查脚本 `scripts/check-ai-fields.mjs`）
- ⏳ **T2.1 云函数代理（开发完成，待真机验收）**：`app/api/chat/route.ts` 代理 MiniMax（密钥仅服务端）+ `lib/server/minimax.ts` 客户端（剥 `<think>`、双重错误检查）+ 前端 send() 接真实 API（带多轮上下文、可停止、错误兜底）。探测结论（`scripts/test-minimax.mjs`）：TokenPlan key 仅国内域名 api.minimaxi.com；可用模型 MiniMax-M2 / MiniMax-Text-01（当前默认 Text-01，响应快输出净；.env 设 MINIMAX_MODEL 可切）。浏览器全链路已验证（`.e2e/chat-real.png`）
- ⏳ **T2.2 System Instruction + 6 变量（开发完成，待真机验收）**：`lib/server/agent.ts`——小涤人设（三大本领+反幻觉铁律）+ ①全馆书单(10分钟缓存) ②已读完(含笔记/书评) ③收藏 ④在读(含笔记/书评) ⑤学习总时长 ⑥压缩历史(T2.6 填充)；身份=前端带 Supabase token、服务端验证；验证 3/3（`scripts/verify-agent-context.mjs`：游客书单 7/7、demo 个人数据/时长/笔记观察、反幻觉）。模型已切 **MiniMax-M3**（用户选定，100万上下文）
- ⏳ **T2.3 工具（4/5 完成，待真机验收）**：`lib/server/tools.ts`——①推荐书目卡片(recs事件→前端拉封面/书名/作者) ②读取书本目录(各章ai_summary，前言无概要兜底) ③读取章内容(≤1.5万字) ④引用章节卡片(cites事件→前端拉章名/摘要，点击跳原文)；Agent 循环最多 5 轮；工具状态文案（查找书籍…/翻阅图书…/章节浏览…）。⑤联网搜索按预案延后（MiniMax 无内置）。验证 3/3（`scripts/verify-agent-tools.mjs`）。调试心得：工具调度规则必须写成"不调用=失败"级别强措辞，M3 才稳定服从
- ⏳ **T2.4 真流式（完成，待真机验收）**：服务端 NDJSON 事件流（d/status/recs/cites/end/err）+ 跨 chunk 剥 `<think>` 过滤器；MiniMax 上游为大块推送（特性），前端做「追赶打字机」平滑渐显（落后越多追越快）；停止=中断请求；首字 ~5s（原整答 11.5s）
- ⏳ **T2.5 对话持久化（完成，待真机验收）**：chat_sessions 写穿透（本地乐观+登录态同步，messages 整段 jsonb、流式临时字段剥离）；登录云端加载+游客会话合并上传；退出仅清本地（云端永久保留）；删除/清空同步云端；赞踩+踩原因随 message 落库。端到端验证通过（`scripts/verify-chat-sync.mjs`：UI登录→对话→落库→清本地后云端回显）
- ⏳ **T2.6 上下文压缩（完成，待真机验收）**：`lib/server/compress.ts`——超过 40 条（20轮）的旧消息异步压成 ≤800 字摘要（Text-01 便宜模型），写 compressed_history/compressed_until，注入 System 变量⑥；原始消息永久保留（压缩≠删除）；工具结果天然不进历史（每请求用完即弃），无需额外清除。验证通过（`scripts/verify-compress.mjs`：60条会话→压缩 until=20→跨压缩记得旧信息）。口径备注：500K token 阈值在本应用以消息条数等价代理
- 验收：真机对话——荐书出推荐卡、答疑/解读出引用卡且跳转正确、流式顺滑可停止、刷新后历史还在、多轮有记忆

## 全面 Review 专项（2026-06-12，三路并行审查共 32 条，T2 验收后插入）

**已修复（commit 9313d18 / c246920 / 3cabce8，回归验证通过）**：
- 🔴 P0×2：①智学流式出错后打字机 interval 泄漏→后续提问永久卡死 ②登录态刷新阅读器/乱翻会用空基线把云端进度/已读章/时长洗掉（hydrated 门禁 + 续读等 load）
- P1：卸载中断误弹错误 toast／重新生成被旧上下文锚定复读／书单查询失败缓存空书单致荐书下线 10 分钟／压缩窗口"上下文黑洞"（裁剪与 KEEP 对齐）／serverless 冻结压缩任务（waitUntil）／跨标签 SIGNED_IN 不加载数据→空基线覆写／换账号对话串号（ownerUid+世代校验）／时长多设备互相覆盖（改增量 RPC）
- P2：上游挂起无超时保护／轮次耗尽丢卡片／退出残留节流定时器误报／getChapter 跨书深链护栏

**SQL 已执行 ✅（2026-06-12 直连自动执行+行为自测通过）**：`add_read_seconds` 增量 RPC + `merge_media_progress`/`merge_text_progress` 触发器（陈旧快照合并实测 pct 取大、已读章并集）。基建升级：`.env.local` 新增 `SUPABASE_DB_URL` 直连串 + `scripts/run-sql.mjs`——**此后全部 SQL 自动执行，无需用户贴 SQL Editor**

**待修队列 → 已全部清零 ✅（commit c936148 / 7286cc2，十路由冒烟全绿）**：
- P1 全修：表单冷加载回填防洗数据／真改密（原密码校验+Auth）／真注销（删号云函数，T4.1+T4.2 提前完成）／热门好书排序用户终裁=入库最早→最近（代码即现状，文档已同步）
- P2 全修：收藏页错误态+防闪骨架／失效书 404 出路／mark 只定位一次／阅读器底栏安全区／乱翻空错态暗底可读（States onDark）／我的评价用当前用户头像昵称／数据卡联动历史页默认筛选（?mode=）／直链返回兜底（useGoBack）／热门搜索改真实书目动态生成／隐私政策云端口径+退出注销文案纠偏／toggleLike 写穿透+likedBooks 死代码清除
- 有意保留：乱翻不显作者（用户此前明确要求去掉，文档口径以此为准）；同会话双标签互覆（远期）；接口限流（上线前 T5）

## 第三批｜T3 乱翻日更 + 热门搜索（中 · 需定时任务）

- ⬜ **T3.1 flip_feed 日更**：Vercel Cron 每天凌晨调 minimax 为每人生成 ≤50 本「猜你想读」（排除已读、在读优先、参考对话偏好）写入 flip_feed（service_role）
- ⬜ **T3.2 getFlip 改读 flip_feed**：50 本轮询续拉；**新用户当天无数据 → 回退最新入库 50 本（has_video=true）**
- ⬜ **T3.3 search_logs 上报**：前端每次有效搜索写一条（游客 user_id 可空）
- ⬜ **T3.4 热门搜索 Top20**：聚合 search_logs（仅保留命中现有书 title/author/tags 的词），替换 mock 的 12 个写死词
- 依赖：Vercel Cron 要等部署后才能真跑——本地先把生成脚本/接口写好，部署后启用
- 验收：乱翻信息流来自 flip_feed；搜几个词后热门搜索出现真实聚合词

## 第四批｜T4 账号小功能补全（小~中）

- ⬜ **T4.1 修改密码**：原密码用 `signInWithPassword` 校验 → `supabase.auth.updateUser({password})`（现为纯本地弹层）
- ⬜ **T4.2 注销账号**：删号云函数 `app/api/account/delete`（service_role 删 auth.users，级联清 profiles/用户数据）
- ⬜ **T4.3 头像上传**：补 `avatars` 桶写策略（本人 `<uid>/` 路径可传改删）+ 前端 storage 上传 + 写 `profiles.avatar_url`（现为本地 dataURL 预览）
- ⬜ **T4.4 意见反馈落库**（可选）：建 feedback 表只写不读；不做则维持现状 toast
- 验收：真机改密后旧密码登不上新密码能登；注销后无法登录且数据清除；换头像后换设备登录仍显示

## 第五批｜T5 阶段4 联调 · 测试 · 上线

- ⬜ **T5.1 全量回归**：按 4 模块过一遍完整流程（游客+登录双角色），含乱翻 100+ 本压力、弱网、微信内置浏览器
- ⬜ **T5.2 push 全部本地提交**（后端 commit 一直没推）
- ⬜ **T5.3 Vercel 部署**：绑定环境变量（NEXT_PUBLIC_SUPABASE_URL/anon 公开；SUPABASE_SERVICE_ROLE_KEY/MINIMAX_API_KEY 仅服务端）；启用 Cron
- ⬜ **T5.4 OSS CORS 核对**：正式域名跨域可播视频/音频/封面
- ⬜ **T5.5 上线后真机验收** + 隐私政策文案更新（「数据存本地」→ 云端口径）
- 验收：线上域名真机全流程跑通 = 项目交付

## 并行内容线｜T6 批量导书（你出内容，我出工具）

- ⬜ **T6.1** 100+ 本书内容生产：每本=文稿(分章 Markdown)+封面+视频+音频 → 传 OSS → `import-books` 脚本入库（已支持干跑校验/覆盖模式）
- ⬜ **T6.2** 导书时自动生成 ai_digest/ai_summary（依赖 T2.0 并入）
- 节奏：随时可做，不阻塞 T1~T5；建议 T2 验收后开始批量灌

## 可选/延后池（不阻塞上线，上线后再议）

- ⬜ PWA 真实图标：192/512 PNG（codex image_gen 生成）替换 icon.svg 占位
- ⬜ 语音输入 ASR（智学长按说话，现为占位 toast）
- ⬜ F68 路由组重构 / F41 长列表虚拟化（工程优化）
- ⬜ 弱网预加载下一条视频 / 清晰度切换
- ⬜ 读者评价（他人书评）公开展示（本版数据-only，将来建只读视图/count RPC）

---

## 推进顺序（定稿）

**T1（快，半天）→ T2（重头，分 7 小步逐步验收）→ T3 → T4 → T5 上线**；T6 内容线并行。
每完成一条：我自测（脚本/真实浏览器验证）→ 你真机验收 → 本文档打勾 ✅ → 下一条。
